import { Inter, League_Spartan, Permanent_Marker } from "next/font/google";

export const permanentMarker = Permanent_Marker({
  subsets: ["latin"],
  weight: "400",
});

export const inter = Inter({
  subsets: ["latin"],
});

export const leagueSpartan = League_Spartan({
  subsets: ["latin"],
  weight: "700",
});
